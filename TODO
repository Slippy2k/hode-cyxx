
verif Game::moveAndyObjectOp3 (> 36)

opcode 223, skipping call to game_unk116
runTask_main_op54() unhandled _gameMstUnk6 -1
runTask_main() unhandled opcode 234

reversing sons / .sss / res_loadSss

option pour demarrer un ecran/level donne ? voir comment c'est gere dans la configuration

game_unk74 / ResLoadOrUnloadPendingDataFromMstLogic

replacer systemstub par libstub
opengl

renommer les structures des donnees deserialisees (en prefixant par le nom du fichier)
	Lvl*
	Sss*
	Dat*
	Mst*

fastcall: _ecx, _edx, stack

if (rcb)
    rtb = rbb;
else
    rtb = rab;

rt = (rb & rc) | (ra & ~rc);

rt = ((ra ^ rb) & rc) ^ ra;

